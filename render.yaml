databases:
  - name: lifehealtherdb
    databaseName: lifehealtherdb
    user: root

services:
  - type: web
    name: lifeHealther
    env: python
    buildCommand: "manage.py"
    startCommand: "runserver"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: lifehealtherdb
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4